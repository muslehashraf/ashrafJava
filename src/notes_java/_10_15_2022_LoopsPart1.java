package notes_java;

public class _10_15_2022_LoopsPart1 {
    /*
            10/15/2022 - Loops Part 1
        Loops: used to execute a block of code n times
        1. For i loop - traditional i loop
        iteration: each cycle that loop goes through
        NOTE: Infinite loop: It is a loop that never stops - It is a loop never reaches the termination condition
        NOTE: For i loop has 3 important statements
        start point: initialization
        end point: termination condition
        update: change (post-increment, post-decrement)
        for (int i = 0; i <= 99; i++){
            System.out.println("Hello World");
        }
        i value  	iteration 		Result
        0 			1 				Hello World
        1 			2 				Hello World
        2 			3 				Hello World
        .
        .
        .
        .
        99 			100 			Hello World
        INFINITE I LOOPS
        for(int i = 0; i >= 0; i++){

        }
        for(;;){

        }
        int age = 25;
        age += 3; // add 3 to the age and reassign it to the age
        age += 5;
        System.out.println(age);// 33
        name.charAt(i) == 'l' || name.charAt(i) == 'L'
        name.toLowerCase().charAt(i) == 'l'
        name.toUpperCase().charAt(i) == 'L'
        2. While loop
        while(condition){
            //statements
        }
        -While loop executes a block of code depending on a condition
        -While condition is true, the block of code will be executed
     */
}
